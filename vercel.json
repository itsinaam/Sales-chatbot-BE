{
    "version": 2,
    "builds": [
        {
            "src": "./app.py",
            "runtime": "python3.11",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/app.py"
        }
    ]
}